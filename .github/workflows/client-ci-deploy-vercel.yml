name: Client CI (No Deploy)

on:
  push:
    branches:
      - main
    paths:
      - "client/**"
      - "workflows/event-microinsurance/**"
      - "workflows/project.yaml"
      - ".github/workflows/client-ci-deploy-vercel.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    name: Status Check
    runs-on: ubuntu-latest
    steps:
      - name: Pass
        run: echo "Skipping deploy and CI validation for this repository."
